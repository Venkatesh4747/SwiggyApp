export interface hotelDetails {
    hotelName: string;
    rating: number;
    imageUrl: string;
    mins: string;
}

export interface popularItem {
    foodName: string;
    imageUrl: string;
}

export interface todayOffer {
    imageUrl: string;
}